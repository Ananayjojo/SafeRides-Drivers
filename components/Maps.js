import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Maps = () => {
  return (
    <View>
      <Text>NavOptions</Text>
    </View>
  )
}

export default Maps
// this file is for the map functionality which is respondible for recieving a location 
//and direct the driver to said destination
// add this to the home page 

const styles = StyleSheet.create({})